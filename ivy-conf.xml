<ivy-conf>
  <properties file="build.properties" />

  <conf defaultResolver="default-resolver" checkUpToDate="true" />

  <resolvers>
      <filesystem name="local">
          <ivy pattern="${ivy.conf.dir}/lib/[module]/apache-ivy-[revision].xml" />
          <artifact pattern="/opt/rh/rh-java-common/root/usr/share/java/[artifact].[ext]" />
          <artifact pattern="/opt/rh/maven30/root/usr/share/java/[artifact].[ext]" />
      </filesystem>

      <ivyrep name="ivyrep" ivyroot="."/>

      <chain name="default-resolver">
          <resolver ref="local" />
          <resolver ref="ivyrep" />
      </chain>
  </resolvers>

</ivy-conf>
